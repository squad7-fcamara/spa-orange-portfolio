import { ContainerLoading, LoadingMessage } from "./StyledLoader";

export default function Loader(){
    return(
        <ContainerLoading>
            <LoadingMessage />
        </ContainerLoading>
    )
}